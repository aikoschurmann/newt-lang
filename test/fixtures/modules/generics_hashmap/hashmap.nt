import std;

pub struct MapEntry<K, V> {
    key: K;
    value: V;
    occupied: bool;
}

pub struct HashMap<K, V> {
    entries: *MapEntry<K, V>;
    size: usize;
    capacity: usize;
}

impl<K, V> HashMap<K, V> {
    pub fn init(self: *HashMap<K, V>, cap: usize) {
        self.capacity = cap;
        self.size = 0;
        self.entries = @alloc(MapEntry<K, V>, std.heap.allocator, cap);
        for (i: usize = 0; i < cap; i = i + 1) {
            self.entries[i].occupied = false;
        }
    }

    pub fn deinit(self: *HashMap<K, V>) {
        @free(std.heap.allocator, self.entries);
    }

    pub fn put(self: *HashMap<K, V>, key: K, value: V) -> bool {
        if (self.size * 2 >= self.capacity) {
            self.resize(self.capacity * 2);
        }

        h: usize = (key as usize) % self.capacity;
        for (i: usize = 0; i < self.capacity; i = i + 1) {
            idx: usize = (h + i) % self.capacity;
            if (!self.entries[idx].occupied) {
                self.entries[idx].key = key;
                self.entries[idx].value = value;
                self.entries[idx].occupied = true;
                self.size = self.size + 1;
                return true;
            }
            if (self.entries[idx].key == key) {
                self.entries[idx].value = value;
                return true;
            }
        }
        return false;
    }

    pub fn get(self: *HashMap<K, V>, key: K) -> V {
        h: usize = (key as usize) % self.capacity;
        for (i: usize = 0; i < self.capacity; i = i + 1) {
            idx: usize = (h + i) % self.capacity;
            if (!self.entries[idx].occupied) {
                break;
            }
            if (self.entries[idx].key == key) {
                return self.entries[idx].value;
            }
        }
        return 0 as V;
    }

    pub fn resize(self: *HashMap<K, V>, new_cap: usize) {
        old_entries: *MapEntry<K, V> = self.entries;
        old_cap: usize = self.capacity;

        self.capacity = new_cap;
        self.size = 0;
        self.entries = @alloc(MapEntry<K, V>, std.heap.allocator, new_cap);
        for (i: usize = 0; i < new_cap; i = i + 1) {
            self.entries[i].occupied = false;
        }

        for (i: usize = 0; i < old_cap; i = i + 1) {
            if (old_entries[i].occupied) {
                self.put(old_entries[i].key, old_entries[i].value);
            }
        }

        @free(std.heap.allocator, old_entries);
    }
}
